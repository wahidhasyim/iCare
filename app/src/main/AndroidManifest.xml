<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.icare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_aplikasi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Daftar" />
        <activity
            android:name=".Chat"
            android:label="@string/title_activity_chat" />
        <activity
            android:name=".Beranda"
            android:label="@string/title_activity_beranda"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".Pilih" />
        <activity android:name=".popup_pilih" />
        <activity
            android:name=".beranda_konselor"
            android:label="@string/title_activity_beranda_konselor" />
        <activity
            android:name=".profile_user"
            android:label="@string/title_activity_profile_user" />

        <activity
            android:name=".profil_konselor"
            android:label="@string/title_activity_profil_konselor" />

        <activity
            android:name=".FriendChat"
            android:label="@string/title_activity_profil_konselor" />
        <activity android:name=".splash_screen"></activity>
    </application>

</manifest>